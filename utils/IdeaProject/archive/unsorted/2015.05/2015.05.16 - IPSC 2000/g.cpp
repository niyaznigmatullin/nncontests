#include <bits/stdc++.h>


using namespace std;

int main() {
    while (scanf("%d", &n) == 1) {
        for (int i = 0; i < n; i++) {
            scanf("%d%d", a + i, b + i);
        }
        
    }
}