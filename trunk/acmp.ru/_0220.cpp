#include <iostream>
#include <stdio.h>

using namespace std;

int main() {
    freopen("input.txt","r",stdin);
    freopen("output.txt","w",stdout);
    cin >> u >> h >> t >> l >> n;
    for (int i=0; i<n; i++) cin >> a[i];
    m=l/t;
    for (int i=0; i<u-h; i++) {
        for (int j=0; j<m; j++) {
            for (int k=0; k<n; k++)
        }
    }
    return 0;
}
